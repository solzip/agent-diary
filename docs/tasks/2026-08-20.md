# 2026-08-20 작업목록

> §0 일일 목록. 커밋하지 않은 로컬 작업 파일이다 (main 직접 커밋 금지 규칙 때문).
> 정본: [`docs/plans/next-session.md`](../plans/next-session.md), 저장소 `CLAUDE.md`.

## 어제 이후 상태 (확인한 것)

- 로컬 커밋 없음. 원격 main은 08-18에 PR #90 머지(시크릿 패턴 수 12로 정정 + 고정 테스트) — **로컬이 2커밋 뒤였고, 오늘 `git pull --ff-only`로 맞췄다** (aac3611 → ebd1cb4)
- 미커밋: 없음 (`.iml`·`.tar.gz`는 gitignore된 로컬 파일, 추적 안 됨 — `git ls-files` 확인)
- 직전 목록 파일: 없음 (docs/tasks/ 오늘 신설)
- CI: 최근 3회 모두 success (gh run list 확인)

## 오늘 한 것

- [x] **프로젝트 전체 점검 (이직용 README·GitHub 설명 검증)** — 결과는 세션 보고 참조.
  요지: GitHub About·topics·homepage 정확, README 두 파일 같은 커밋으로 동기화 유지 중,
  참조 링크 12개 전부 실존, 예시 항목 구조를 `agent-diary try` 실출력과 대조해 일치 확인,
  로드맵의 #12 아직 open이라 정확, 배지(py3.8+/MIT/CI) 실제와 일치

## 잔여 (무엇이 있어야 닫히는가)

| 항목 | 분류 | 차단 요인 |
|---|---|---|
| PR #91 머지 (CHANGELOG 2.0.0 패턴 수 9로 정정) | 결정 | Sol의 머지 판단. CI green |
| PyPI `claude-diary` 4.2.0 yank | 외부 대기 | PyPI 자격증명 — Sol만 가능 (next-session.md 「Waiting on Sol」) |
| Notion 스키마 축소 (#12) | 코드 | 없음. 착수 순서는 next-session.md 「what order to remove things in」 준수 |
| records/work-items 남은 결정 3건 (배포 마커 형태 등) | 결정 | next-session.md 「Still open」 참조 |

## 점검에서 미검증으로 남긴 것

- ~~demo.svg 내용 렌더링~~ → 후속에서 완료
- ~~README 전 문장 대 코드 전수 대조~~ → 후속에서 완료
- ~~PyPI 프로젝트 페이지의 README 렌더링 상태~~ → 후속에서 완료

## 후속 (2026-08-20, 같은 세션)

미검증 3건을 마저 확인했다. 상세는 세션 보고 참조. 요지:

**PyPI 렌더링** — `readme_renderer[md]`(PyPI가 쓰는 그 라이브러리)로 4.12.0 설명을
렌더링해 확인. 마크다운 자체는 정상 렌더링되지만:
- `![demo](docs/demo.svg)` 상대경로 → PyPI에서 **이미지 깨짐**
- 상대 링크 12개(ARCHITECTURE·postmortem·ADR 등 — 이직용 핵심 링크들) → PyPI에서 전부 죽은 링크
- raw HTML 앵커 `#reindex-once-on-4-11-3` → PyPI에서 깨짐 (GitHub에선 정상)
- PyPI 설명은 4.12.0 시점 스냅샷이라 "11+ patterns" 그대로 (다음 릴리스에서 자동 반영)

**demo.svg** — 항목 미리보기 라벨은 현행 포맷터와 일치. 단 "── Claude Code
session ends ──" 문구가 4.9.0 이전의 세션 단위 서술 (README가 #83에서 고친
바로 그 드리프트). `[diary] Session #3 ...` 출력도 훅이 실제로 찍지 않는 연출.

**README 전수 대조 (에이전트 4개, file:line 근거)** — 대부분 VERIFIED.
불일치 발견분:
- 코드 결함 의심 2건 (재현 전, §4 규칙상 미수정):
  ① 브랜치 차수 `(#N)` — `core.py:127`이 현재 세션을 카운트에서 빼지 않아
  세션의 2번째 턴부터 +1 (4.12.0이 고친 그 자리의 잔여 결함)
  ② `team monthly --month` — 플래그를 받고 무시, weekly와 같은 함수 호출
- `/diary`가 `--input` 없이 돌아 📝 Work Summary를 만들 수 없음 — README 64행과 모순
- README 366행 "5 core views and 5 operating views" — 실제 3+2 (`notion_views.py:23-25`), `setup.py:55`도 동일 오기
- §2-8 Sub-items 미활성 시 "시각적 중첩만 빠진다" — 실제로는 parented task 실패 처리, exit 1
- 기타 경미: dry-run manifest에 results 없음, `--preview-file`이 실제 push에서 무시됨, 시크릿 마스킹 "in Notion" 문구가 diary-notion push 경로엔 해당 안 됨

**차단 요인**: 위 수정은 전부 Sol 판단 대기 (README 수정 PR / 코드 결함은 재현 먼저).

## 후속 2 (2026-08-20, 같은 세션) — Sol 승인으로 1·2·3 전부 수정 착수

PR 4개 생성, 규칙 준수: 작업당 브랜치 1개, 결함은 재현 테스트 먼저, 두 README 동시 수정.

| PR | 내용 | 검증 |
|---|---|---|
| #92 docs | README 상대링크 12개→절대 URL, demo 이미지 raw URL, 이중 앵커, demo.svg 문구 2건 | readme_renderer로 before/after 렌더 비교 (상대링크·깨진앵커 0) |
| #93 docs | README 서술 5건을 코드 실동작에 맞춤 + setup.py·Codex 스킬 사본 문구 동기화 | 1,203 passed (동기화 테스트가 사본 누락을 실제로 잡음) |
| #94 fix | 브랜치 차수 off-by-one — `count_branch_sessions(exclude_session_id=)` | 재현 테스트 3개 선작성(실패 확인) → 1,206 passed |
| #95 fix | `team monthly --month` 무시 — `team_monthly_report` 신설, `monthly/team-YYYY-MM.md` | 재현 테스트 4개 선작성(실패 확인) → 1,207 passed |

- #95 명명 판단(`monthly/` 디렉터리, 파일명)은 weekly 대칭으로 정하고 PR 본문에 검토 요청 명시
- CHANGELOG [Unreleased]에 #93·#94·#95 항목 추가 — 세 PR이 같은 지점을 수정하므로 **머지 순서대로 충돌 해소 필요**
- PyPI 반영은 다음 릴리스에서. #91(기존 PR)은 Sol 판단 그대로 대기
- CI 대기 → green이면 순서대로 머지 + 브랜치 삭제 예정

## 후속 3 (2026-08-20, 같은 세션) — 머지 완료, 묶음 종료

- #92 → #93 → #94 → #95 순서로 전부 머지 (Sol 승인: "ci 통과하면 머지까지").
  CHANGELOG [Unreleased] 충돌 2회는 브랜치에 main을 머지해 항목 3개를 쌓는 방식으로 해소,
  해소 후 CI 재green 확인하고 머지
- 머지된 main 최종 확인: **1,210 passed** (#94의 3개 + #95의 4개 신규 테스트 포함)
- 작업 브랜치 로컬·원격 모두 삭제됨. 남은 원격 브랜치는 `docs/the-2-0-0-pattern-count-was-nine`(#91, Sol 판단 대기)뿐

### 다음 세션 인계

- **읽기 시작점**: 이 파일 + `docs/plans/next-session.md` (단, next-session.md의
  "CHANGELOG [Unreleased] empty" / "branch ordinal" 서술은 오늘 이후 stale — 갱신 후보)
- **남은 것**:
  1. PR #91 머지 여부 (Sol 판단, CI green)
  2. **릴리스** — [Unreleased]에 Fixed 3건이 쌓여 있고, PyPI의 README(깨진 링크 포함)는
     릴리스가 나가야 갱신된다. 차수 off-by-one 수정도 릴리스 전까지는 사용자에게 미전달
  3. PyPI `claude-diary` 4.2.0 yank (Sol만 가능)
  4. `next-session.md` 갱신 (오늘 변경 반영) — 별도 docs PR 필요
  5. 후보 논의: `/diary`에 요약 경로 추가할지, `diary-notion push`에 시크릿 스캔 태울지 (#93 본문 참조)
