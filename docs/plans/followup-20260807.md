# Follow-up — 2026-08-07 세션 이후 남은 작업

> Notion 업무일지 가독성 개선 세션에서 넘긴 항목. 우선순위순.

## P0 — 데이터 복구 (시간제한 있음)

`ensure`의 스키마 PATCH 결함으로 2026 DB 497 row에서 6개 select 속성이 지워졌다
(`Status` `Purpose` `Task Group` `Priority` `Review Status` `Schema Version`).
원인은 `1efc86b`에서 수정됐고 더 이상 발생하지 않는다. 남은 건 복구.

- [ ] **노션 페이지 히스토리 확인** — 요금제에 따라 7~30일 창. 이게 488 row의 유일한 복구 경로이고 **가장 급하다**
- [ ] 오늘 push한 9건 중 **8건 복원** — `.codefleet/runs/20260807-172609-*/input.json`에 값 보존. idx 2는 복원 완료
- [ ] 2026-06-05 codex 2세션 복원 — `.codefleet/runs/20260605-*/input.json` 보존

손실되지 않은 것: 페이지 본문 전체, 모든 relation(하위항목 포함),
`Name` `Date` `Project` `Branch` `Categories` `Files` `Commits` `Lines`
`Session ID` `Task Index` `Work Period`.

## P1 — 속성 과다 (사용자 지적, 미해결)

뷰 컬럼 축소는 **표에서만** 적용된다. row를 열면 노션이 속성 패널에 28개를 전부 보여준다.
"뷰에서 숨기기만" 방식의 명시된 한계이며, 없애려면 스키마를 실제로 줄여야 한다.

- [ ] 스키마 v9 축소 — 삭제 후보: `Work Period`(Date와 중복), `Blocked`(Block Reason으로 추론 가능),
      `Carryover`(Task Group으로 추적), `Branch`(카디널리티 높은 select), `Files`/`Commits`/`Lines`(본문 중복)
- [ ] 유지 필수: `Session ID` `Task Index`(멱등성·정렬), `Schema Version`
- [ ] 기존 DB 마이그레이션 경로 필요 — **P0 복구가 끝난 뒤에 착수할 것**

## P2 — 배포

- [ ] `git push` + main 대상 PR (main 대비 21커밋). CI 트리거를 고쳤으므로 push만으로 실행됨
- [ ] 노션에서 잔여 뷰 5개 수동 삭제: `상태별` `목적별` `프로젝트별` `오늘 우선순위` `리뷰 필요`
      (자동 삭제는 의도적으로 막아둠 — 손으로 커스터마이즈한 레이아웃 보호)

## P3 — 알려진 이슈

- [ ] **dry-run에 차수 미반영** — 미리보기는 자격증명 확인 전에 끝나므로 `(N차)`가 안 붙어 실제 push 결과와 다름
- [ ] **Task Index는 push마다 리셋** — 같은 날 두 번 push하면 순서가 섞인다.
      전역 정렬이 필요하면 노션 내장 `Created time`을 숨김 속성으로 추가
- [ ] **`review` 큐가 비어 있음** — `Review Status`가 유실돼 검토 대상이 잡히지 않는다. P0 복구에 종속
- [ ] `notion_push.py` 1000줄 — validate / properties / artifacts / report로 분할

## 검증 대기

- [ ] **차수 `(N차)` 실동작** — 이번 세션은 두 그룹 다 1차였고 1차는 접미사를 붙이지 않는다.
      다음 세션에 같은 `task_group`으로 push할 때 `(2차)`가 붙는지 확인
