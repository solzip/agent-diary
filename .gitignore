# IDE
.idea/
*.iml
.vscode/

# Python
__pycache__/
*.pyc
*.egg-info/
dist/
build/
# Throwaway environments. One of these was created inside packaging/ while
# checking that the redirect release resolves, and 799 files of it reached a
# commit before anyone looked.
.venv/
venv/
*-test/pyvenv.cfg
redirect-test/
.pytest_cache/
.coverage
htmlcov/

# bkit plugin state
.bkit/

# Claude Code project data
.claude/

# OS
Thumbs.db
.DS_Store

# Original archive (source already extracted)
claude-code-working-diary.tar.gz

# /diary-notion 임시 JSON (Claude가 cwd에 작성 → CLI가 자동 삭제 / 부분 실패 시 보존)
.diary-notion-*.json

# Local v2 diary-notion run artifacts
.agent-diary/runs/
.codefleet/runs/
