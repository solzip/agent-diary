interface:
  display_name: "Diary"
  short_description: "Record a Codex session to Markdown"
  default_prompt: "Use $diary to record this session in my working diary."

policy:
  allow_implicit_invocation: true
